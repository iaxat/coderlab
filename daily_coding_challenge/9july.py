# Easy Amazon

class Solution():
    def __init__(self, encoding_code) -> None:
        self.encoding_code = encoding_code
        self.results = []

    def encoding(self):
        temp_counter = 0
        len_str = len(self.encoding_code)
        if self.encoding_code and len_str>1:
            for i in range(0,len_str):
                if i 


            return ''.join(self.results)
        return self.encoding_code

    def decoding(self):




encoding_code = "AAAABBBCCDAA"
decoding_code = 
sol = Solution(encoding_code)
sol.encoding()
sol.decoding()
